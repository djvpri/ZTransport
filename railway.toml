# Paksa Dockerfile builder — cegah Railpack fallback (pola ekosistem Zomet).
[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "npm run start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
